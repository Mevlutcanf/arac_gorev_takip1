# Git Workflow Rules
- Do NOT automatically push changes to GitHub (`git push`).
- Wait for explicit user confirmation before committing and pushing.
- Always assume the user wants to test changes locally first.
